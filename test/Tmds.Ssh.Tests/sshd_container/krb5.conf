[libdefaults]
 default_realm = REALM.TEST
 dns_lookup_realm = false
 dns_lookup_kdc = false

[realms]
  REALM.TEST = {
    kdc = localhost
 }

[domain_realm]
  .realm.test = REALM.TEST
  realm.test = REALM.TEST